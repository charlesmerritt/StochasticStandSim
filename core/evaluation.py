""" Common metrics and plotting-agnostic summaries.

KPIs: mean return, CVaR of return at alpha, timber yield, carbon, variance, disturbance loss stats.

evaluate_policies(env, policies, episodes, steps, alpha) -> DataFrame """